# Caminho ABSOLUTO da raiz do dataset (formato COCO).
# Copie este arquivo para .env e ajuste o valor.
# Estrutura esperada:
#   $DATASET_PATH/train/            (imagens de treino)
#   $DATASET_PATH/train_coco.json   (anotacoes COCO de treino)
#   $DATASET_PATH/valid/            (imagens de validacao)
#   $DATASET_PATH/valid_coco.json   (anotacoes COCO de validacao)
DATASET_PATH=/home/joao/Downloads/MobIA 5.4.coco/proto